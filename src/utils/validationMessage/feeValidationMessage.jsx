const feeTypeSelectionRequired = `fee type is required`;
const monthlyFeeRequired = `monthly fee is required`;
const monthlyFeeMinValue = `monthly fee must be at least 1 rupee`;
const monthlyFeeIntegerNumber = `monthly fee must be an integer number`;
const monthlyFeeMaxValue = `monthly fee must be at most 100000 rupees`;
const monthlyFeePositiveNumber = `monthly fee must be a positive number`;
const quarterlyFeeRequired = `quarterly fee is required`;
const quarterlyFeeMinValue = `quarterly fee must be at least 1 rupee`;
const quarterlyFeeIntegerNumber = `quarterly fee must be an integer number`;
const quarterlyFeeMaxValue = `quarterly fee must be at most 100000 rupees`;
const quarterlyFeePositiveNumber = `quarterly fee must be a positive number`;
const halfYearlyFeeRequired = `halfYearly fee is required`;
const halfYearlyFeeMinValue = `halfYearly fee must be at least 1 rupee`;
const halfYearlyFeeIntegerNumber = `halfYearly fee must be an integer number`;
const halfYearlyFeeMaxValue = `halfYearly fee must be at most 100000 rupees`;
const halfYearlyFeePositiveNumber = `halfYearly fee must be a positive number`;
const yearlyFeeRequired = `yearly fee is required`;
const yearlyFeeMinValue = `yearly fee must be at least 1 rupee`;
const yearlyFeeIntegerNumber = `yearly fee must be an integer number`;
const yearlyFeeMaxValue = `yearly fee must be at most 100000 rupees`;
const yearlyFeePositiveNumber = `yearly fee must be a positive number`;

export {
  feeTypeSelectionRequired,
  monthlyFeeRequired,
  monthlyFeeMinValue,
  monthlyFeeMaxValue,
  monthlyFeeIntegerNumber,
  monthlyFeePositiveNumber,
  quarterlyFeeRequired,
  quarterlyFeeMinValue,
  quarterlyFeeMaxValue,
  quarterlyFeeIntegerNumber,
  quarterlyFeePositiveNumber,
  halfYearlyFeeRequired,
  halfYearlyFeeMinValue,
  halfYearlyFeeMaxValue,
  halfYearlyFeeIntegerNumber,
  halfYearlyFeePositiveNumber,
  yearlyFeeRequired,
  yearlyFeeMinValue,
  yearlyFeeMaxValue,
  yearlyFeeIntegerNumber,
  yearlyFeePositiveNumber,
};
